import tkinter as tk
from tkinter import messagebox
import random
#usamos tkinter para poder crear la interfaz grafica
#el messagebox lo importamos porque es un modulo de tkinter que nos permite mostrar ventanas en este caso como alerta de error si en este caso en vez de escribir numeros escribimos con letras este nos tiraria un error
# y el random lo usamos para generar numeros aleatorios
